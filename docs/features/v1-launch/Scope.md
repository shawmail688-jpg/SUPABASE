# 范围界定（Scope）

> 用途：划界 + 可自动化验收标准（C6）。需求条目见同目录 Requirement.md（R1-R18）。

Version：1.0
Status：Approved（L1，用户 2026-09-03 一句话确认；范围蔓延一律走 C2 变更路径）
Author：Claude
Last Update：2026-09-03

---

# 一、范围内（做）与验收标准

## 阶段一（M1→M3a，≈6.5-7.5 工作日，领导可见最小闭环）

| 需求编号 | 里程碑 | 验收标准（可自动化） |
|----------|--------|----------------------|
| R1 R2 R3 R4 R5 R17 | M1 数据层+状态机 | ① `supabase/migrations/*.sql` 在干净 Supabase 项目按序重放，退出码 0；② 权限矩阵测试脚本（三角色×操作）全过；③ 状态机非法转换用例全拒；④ `external_ids` / `site_status_log` 存在且触发器生效断言过 |
| R6 | M1 存量迁移 | 对账脚本输出 `rows_match: true`（行数+逐行 sha1 零差异），报告存档 |
| R7 | M1 备份 | 计划任务列表可见每日任务；恢复演练：pg_restore 到临时库 → 行数对账脚本零差异，记录存档 |
| R8 | M1 审计 | 触发器用例：改 site 一行 → audit_log 断言旧值/新值/who |
| R9 R10 R12 | M2a 照片上云 | ① 未签名 GET 返回 4xx；② 压缩脚本对样本照片输出：长边 ∈ [1600,2000] 且体积 < 1MB（细节照档除外，上限 2.5MB）；③ 断网→回网上传用例过；④ 存量照片迁移后 Storage 对象计数=photo 表计数 |
| R11 | M2b 表单换靶 | ① 双写 ≥7 天对账脚本零差异；② 退役 checklist 执行记录；③ 换靶后表单 E2E：在线提交→PG 可查、离线攒单→回网同步落库 |
| R13 R14 R16 R18 | M3a 领导视图 | ① 匿名访问看板→不可见数据（断言）；② 自检脚本（沿用无头 Edge #/selftest 模式）覆盖汇总条筛选/pin→卡片/批准+隐藏按钮可见性，全绿；③ 领导手机实开验收记录；④ 领导账号批准一条→site_status_log 出现对应行（闭环证据） |

## 阶段二（M3b，≈2-3 工作日，完整工作台）

| 需求编号 | 里程碑 | 验收标准（可自动化） |
|----------|--------|----------------------|
| R15 | M3b 工作视图 | E2E 五操作：查（表格渲染行数=DB 数）、改（编辑字段→DB 值断言）、批（同 R14 闭环）、藏+恢复（仅 admin 入口，非 admin DOM 不含）、导出（CSV 行数=筛选结果数） |

## 横切（随里程碑交付）

| 需求编号 | 内容 | 验收标准 |
|----------|------|----------|
| N1 | 安全五层 | 上述权限/签名/匿名用例 + 安全层代码送 ChatGPT 手术式外审一次，处置表存档 |
| N2 | 离线灵魂 | R11 断网用例 + 既有管线离线测试回归全绿 |

# 二、范围外（明确不做）

| 项 | 原因 |
|----|------|
| M5 监控嵌入（device 表+台账+H5 播放） | 后续项目（09-03 用户定案），IT 盘点答复后按 §4.2 分档另行立项 |
| 总部 API 接入 + 致国内开发信函 | 归档轨道（§11.2），触发条件恢复时单独立项 |
| Sheets 双向同步/工作台化（原 M4） | 09-03 取消：管理操作全走网页；§9 三规则降级备轨 |
| 在营（operating）状态启用 | 枚举预留不启用，未来总部数据自动点亮 |
| Import/Export Job 体系、Role+Scope 组织树权限、字段级冲突裁决 | 千店级组件，十几人规模不采纳（附录 A 两轮处置留痕） |
| 财务数据接入 | 超出平台边界（§1 数据归属表） |

# 三、延后（以后做）

| 项 | 计划版本 |
|----|----------|
| M5 监控（A/B/C 三档方案已备 §4.2） | 后续项目，IT 盘点后立项评估 |
| 总部 API 代理+映射落库 | 归档轨道恢复时 |
| Sheets 投影备轨（按 §9 三规则，约 2 天） | 用户重新提出时 |
| device 表 | 随 M5 |

# 四、版本边界

- 本范围所属版本：**v1-launch**（阶段一+阶段二，≈9-11 工作日）
- 交付后动作：阶段二交付完成→例行复评一次（Assessment §5 触发条件）；监控/总部读取单独立项不回填本范围

# 五、风险项

| 风险 | 影响 | 缓解 |
|------|------|------|
| Supabase 为团队首个承重云依赖（选型已论证、原型未做） | M1 期踩坑 | Assessment 硬触发已定 Large；M1 首日先跑通最小闭环（建项目→建 1 表→RLS 用例）再铺开 |
| 存量迁移对账不平 | 上线数据不可信 | sha1 逐行对账脚本前置，零差异才切；差异清单人工裁决 |
| Apps Script 双写期漏写/错写 | 数据分叉 | 对账脚本每日跑，零差异才退役；退役后旧通道关闭 |
| 离线表单换靶引入回归 | 调查员野外不可用=事故 | N2 硬约束：断网用例+既有测试回归全绿才算过 |
| 办公室机器宕机 | 每日冷备停 | 看板不受影响（云上）；恢复即补跑，TECH_DEBT 登记 |
| Supabase 合规不通过（总部答复前提未回） | 换底座 | 标准 Postgres+pg_dump 平迁自建 HK，架构不变（§5 备选已留） |
| 多模型协作互相改坏 | 返工 | git 分支/PR/审后合 + 验收条件先行 + ChatGPT 安全层外审 |

# 六、范围变更记录

- （C2 变更路径触发时在此追加：日期 / 变更内容 / 影响 / 批准人）
